{
  "buildCommand": "pnpm run build",
  "outputDirectory": "packages/client/dist",
  "functions": {
    "api/**/*.ts": {
      "runtime": "edge"
    }
  },
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/:path*"
    }
  ]
}
